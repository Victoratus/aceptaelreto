#include <stdio.h>
#include <string.h>
#include <iomanip>


int main() {
    char str[1001];
    int aux;
    while (true) {
        scanf("%s", str);
        if (feof(stdin)) return 0;
        str = 
    }

}